import SwiftUI

struct ZwaaiRuimte: View {
    var body: some View {
        Text("Zwaai bij ruimte")
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(Color(.background))
        .navigationBarTitle("Ruimte")
    }
}
